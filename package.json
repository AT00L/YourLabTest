{
  "name": "yourlabtest-monorepo",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "pnpm run dev:client & pnpm run dev:server",
    "build": "pnpm --filter client build"
  }
}
